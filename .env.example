# Server
PORT=3000
NODE_ENV=development

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=electropi_db

# JWT Secret
JWT_SECRET=super_secret_jwt_key_for_electro_pi
JWT_EXPIRES_IN=1d